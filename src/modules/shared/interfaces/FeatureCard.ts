export interface FeatureCardType {
    image: string;
    title: string;
    description: string;
    buttonText: string;
    buttonIcon: React.ReactNode;
    buttonColor: string;
    saleTag: React.ReactNode;
}